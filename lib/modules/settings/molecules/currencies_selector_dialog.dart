import 'package:flutter/material.dart';

class CurrenciesSelectorDialog extends StatelessWidget {
  const CurrenciesSelectorDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return SimpleDialog();
  }
}
